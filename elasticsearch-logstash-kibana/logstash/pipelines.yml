- pipeline.id: distributor
  queue.type: persisted
  path.config: /usr/share/logstash/pipeline/distributor.conf
- pipeline.id: json
  queue.type: persisted
  path.config: /usr/share/logstash/pipeline/json.conf
# - pipeline.id: type2
#   queue.type: persisted
#   config.string: |
#     input { pipeline { address => type2 } }
#     filter {
#        # type2 filter statements here...
#     }
#     output {
#       # type2 output here...
#     }
# - pipeline.id: fallback
#   queue.type: persisted
#   config.string: |
#     input { pipeline { address => fallback } }
#     filter {
#        # fallback filter statements here, if any
#     }
#     output {
#       # fallback output here...
#     }
